export type MemberType = {
  name: string;
  position: string;
  image: string;
};

export type CommitteeType = {
  title: string;
  description: string;
  image: string;
};

export type EventType = {
  title: string;
  description: string;
  image: string;
};

export type Link = {
  name: string;
  link: string;
};
